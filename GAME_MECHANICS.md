# Подробное описание механики игры "Симулятор инвестора в недвижимость Мурманска"

## 📋 Оглавление
1. [Общая концепция](#общая-концепция)
2. [Игровые сущности и типы данных](#игровые-сущности-и-типы-данных)
3. [Статические переменные и константы](#статические-переменные-и-константы)
4. [Игровой цикл (обработка месяца)](#игровой-цикл-обработка-месяца)
5. [Механики покупки и продажи](#механики-покупки-и-продажи)
6. [Система кредитования](#система-кредитования)
7. [Ремонт и улучшение объектов](#ремонт-и-улучшение-объектов)
8. [Рыночная динамика](#рыночная-динамика)
9. [Система рисков](#система-рисков)
10. [Геймификация: уровни, миссии, достижения](#геймификация-уровни-миссии-достижения)
11. [Расширенные рыночные события](#расширенные-рыночные-события)
12. [Переговоры и торг](#переговоры-и-торг)

---

## Общая концепция

Игра представляет собой симулятор инвестиций в недвижимость города Мурманска. Игрок управляет портфелем недвижимости, покупая объекты, сдавая их в аренду или перепродавая, управляя кредитами и рисками.

**Ключевые особенности:**
- Время идет автоматически: 1 реальная минута = 1 игровой месяц
- Игра бессрочная (нет финального месяца)
- Все данные хранятся в памяти (без бэкенда)
- Динамический рынок с фазами роста, стабильности и кризиса
- Система уровней и достижений для мотивации

---

## Игровые сущности и типы данных

### 1. Игрок (Player)

```typescript
interface Player {
  id: string;                    // Уникальный идентификатор
  name: string;                  // Имя игрока
  cash: number;                  // Свободные деньги (₽)
  netWorth: number;              // Чистый капитал (рассчитывается)
  loans: Loan[];                 // Список кредитов
  properties: Property[];        // Список объектов недвижимости
  currentMonth: number;          // Текущий игровой месяц (начинается с 0)
  difficulty: Difficulty;         // Сложность: "easy" | "normal" | "hard"
  totalMonths: number;           // Общее количество месяцев игры
  experience: number;            // Опыт игрока (для уровней)
  level: number;                 // Текущий уровень (1-10)
  stats: {
    totalSales: number;          // Всего продано объектов
    totalRentIncome: number;     // Всего получено аренды
    totalRenovations: number;   // Всего сделано ремонтов
    propertiesOwned: number;     // Максимум одновременно владеемых объектов
  };
}
```

### 2. Объект недвижимости (Property)

```typescript
interface Property {
  id: string;                    // Уникальный идентификатор
  name: string;                  // Название объекта
  district: District;            // Район: "Центр" | "Спальный район" | "Возле порта" | "Отдалённый район"
  type: PropertyType;            // Тип: "Квартира" | "Студия" | "Коммерция" | "Комната"
  purchasePrice: number;         // Цена покупки (неизменна)
  currentValue: number;          // Текущая рыночная стоимость (меняется)
  baseMonthlyRent: number;       // Базовая ставка аренды
  condition: PropertyCondition;  // Состояние: "убитая" | "требует ремонта" | "нормальная" | "после ремонта"
  strategy: PropertyStrategy;     // Стратегия: "hold" | "rent" | "flip" | null
  monthlyExpenses: number;       // Ежемесячные расходы на содержание
  mortgageId?: string;           // ID кредита, если объект в ипотеке
  monthsOwned: number;           // Сколько месяцев во владении
  isForSale: boolean;            // Выставлен ли на продажу
  salePrice?: number;            // Цена, по которой выставлен на продажу
  isUnderRenovation?: boolean;   // Идет ли ремонт
  renovationMonthsLeft?: number; // Сколько месяцев осталось до завершения ремонта
}
```

### 3. Кредит (Loan)

```typescript
interface Loan {
  id: string;                    // Уникальный идентификатор
  propertyId?: string;           // ID объекта, под который взят кредит
  principal: number;             // Тело кредита (первоначальная сумма)
  interestRate: number;          // Годовая процентная ставка (%)
  monthlyPayment: number;        // Ежемесячный платёж (аннуитет)
  remainingTermMonths: number;   // Осталось месяцев до погашения
  type: LoanType;                // Тип: "ипотека" | "залог" | "потребкредит"
  remainingPrincipal: number;    // Остаток по кредиту
}
```

### 4. Рыночное состояние (MarketState)

```typescript
interface MarketState {
  currentPhase: MarketPhase;     // Фаза: "рост" | "стабильность" | "кризис"
  priceIndex: number;            // Индекс цен (множитель, 1.0 = нейтрально)
  rentIndex: number;             // Индекс аренды (множитель, 1.0 = нейтрально)
  vacancyRate: number;           // Процент простоя (0.05 = 5%)
  activeEvents: MarketEvent[];   // Активные рыночные события
}
```

### 5. Рыночное событие (MarketEvent)

```typescript
interface MarketEvent {
  id: string;                    // Уникальный идентификатор
  name: string;                  // Название события
  description: string;            // Описание
  monthImpactStart: number;      // С какого месяца начинает действовать
  durationMonths: number;         // Длительность в месяцах
  priceImpactPercent: number;    // Влияние на цены (%)
  rentImpactPercent: number;     // Влияние на аренду (%)
  vacancyImpactPercent: number;  // Влияние на простой (%)
}
```

---

## Статические переменные и константы

### Начальный капитал по сложности

```typescript
startingCashByDifficulty = {
  easy: 2_000_000 ₽,    // Легкая сложность
  normal: 1_500_000 ₽,   // Нормальная сложность (по умолчанию)
  hard: 1_000_000 ₽      // Сложная сложность
}
```

### Параметры кредитования по сложности

```typescript
loanPresetsByDifficulty = {
  easy: {
    baseInterestRate: 9.5%,    // Базовая ставка по кредиту
    maxLtv: 0.8,               // Максимальная доля кредита от стоимости (80%)
    description: "Мягкий рынок кредитования, низкие ставки."
  },
  normal: {
    baseInterestRate: 12.5%,
    maxLtv: 0.75,               // 75%
    description: "Обычные условия кредитования."
  },
  hard: {
    baseInterestRate: 15.5%,
    maxLtv: 0.7,                // 70%
    description: "Жёсткие условия: высокие ставки, меньше доступный кредит."
  }
}
```

### Параметры ипотеки

- **Первоначальный взнос**: 20% от стоимости объекта
- **Срок кредита**: 120 месяцев (10 лет)
- **Тип платежа**: Аннуитетный

### Параметры залога под объект

- **Максимальная сумма**: 60% от текущей стоимости объекта
- **Срок кредита**: 60 месяцев (5 лет)
- **Процентная ставка**: Базовая ставка + 2%
- **Тип платежа**: Аннуитетный

### Стоимость ремонта

```typescript
renovationCosts = {
  косметика: purchasePrice * 0.05,    // 5% от покупной цены
  капремонт: purchasePrice * 0.15      // 15% от покупной цены
}

renovationDuration = {
  косметика: 1 месяц,
  капремонт: 3 месяца
}
```

### Влияние ремонта на стоимость

- **Косметический ремонт**: +10% к текущей стоимости
- **Капитальный ремонт**: +20% к текущей стоимости

### Улучшение состояния после ремонта

```
"убитая" → "требует ремонта" → "нормальная" → "после ремонта"
```

### Налог на прибыль при продаже

- **Ставка**: 13% от прибыли
- **Прибыль** = Цена продажи - Цена покупки - Стоимость ремонта
- Если прибыль ≤ 0, налог не взимается

### Вероятность продажи (flip стратегия)

```typescript
saleChance = {
  priceRatio <= 0.95: 0.5,    // Дешевле рынка на 5%+ → 50% шанс
  priceRatio <= 1.0: 0.3,    // На уровне рынка → 30% шанс
  priceRatio <= 1.1: 0.15,  // Дороже на 10% → 15% шанс
  priceRatio > 1.1: 0.05    // Значительно дороже → 5% шанс
}
```

### Вероятность риска на объекте

- **Базовая вероятность**: 5% каждый месяц
- Не проверяется для объектов на продаже или в ремонте

### Вероятность смены рыночной фазы

- **Базовая вероятность**: 5% каждый месяц
- При смене случайно выбирается одна из трех фаз

---

## Игровой цикл (обработка месяца)

Каждый месяц выполняется следующая последовательность действий:

### 1. Обновление рынка

```typescript
// Обновление фазы рынка (5% шанс смены)
market.currentPhase = updateMarketPhase(market.currentPhase);

// Обновление индексов в зависимости от фазы
market = updateMarketIndexes(market);

// Активация новых событий
market = checkAndActivateEvents(market, player.currentMonth);
```

**Изменение индексов по фазам:**

- **Рост**:
  - `priceIndex`: ×1.005 (до максимума 1.3)
  - `rentIndex`: ×1.003 (до максимума 1.2)
  - `vacancyRate`: ×0.99 (минимум 0.02)

- **Стабильность**:
  - `priceIndex`: Стремление к 1.0
  - `rentIndex`: Стремление к 1.0
  - `vacancyRate`: Стремление к 0.05

- **Кризис**:
  - `priceIndex`: ×0.995 (минимум 0.7)
  - `rentIndex`: ×0.997 (минимум 0.8)
  - `vacancyRate`: ×1.02 (максимум 0.15)

### 2. Обновление стоимости объектов

```typescript
newValue = currentValue * phaseMultiplier * (1 + eventImpacts) * priceIndex
// Минимум: purchasePrice * 0.7 (не может упасть ниже 70% от покупной цены)
```

### 3. Обработка ремонта

- Уменьшается `renovationMonthsLeft`
- При завершении:
  - Улучшается `condition`
  - Увеличивается `currentValue` (если еще не было увеличено при старте)

### 4. Обработка продаж (flip стратегия)

Для каждого объекта со стратегией "flip" и `isForSale = true`:

1. Рассчитывается вероятность продажи на основе `salePrice / marketPrice`
2. Если продажа произошла:
   - Рассчитывается налог на прибыль (13%)
   - Выплачивается кредит, если есть
   - Объект удаляется из портфеля
   - Начисляется опыт: +50 XP
   - Обновляется статистика: `totalSales += 1`

### 5. Начисление аренды

Для каждого объекта со стратегией "rent" и не в ремонте:

```typescript
rent = baseMonthlyRent * rentIndex * (1 + eventImpacts)
// Проверка вакансии (вероятность = vacancyRate)
if (random < vacancyRate) {
  income = 0; // Арендатор съехал
} else {
  income = rent - monthlyExpenses;
}
```

- Начисляется опыт: `Math.floor(income / 1000)` XP
- Обновляется статистика: `totalRentIncome += income`

### 6. Списывание расходов

```typescript
totalExpenses = sum(property.monthlyExpenses) + sum(loan.monthlyPayment)
player.cash -= totalExpenses
```

### 7. Обработка кредитов

Для каждого кредита:
- Списывается `monthlyPayment`
- Рассчитывается процент: `remainingPrincipal * (interestRate / 100 / 12)`
- Уменьшается `remainingPrincipal`
- Уменьшается `remainingTermMonths`

Погашенные кредиты (remainingPrincipal ≤ 0) удаляются.

### 8. Проверка банкротства

Если `player.cash < 0`:
- Создается событие с предупреждением
- Игрок может продать активы или взять кредит

### 9. Обновление чистого капитала

```typescript
netWorth = cash + sum(property.currentValue) - sum(loan.remainingPrincipal)
```

### 10. Переход к следующему месяцу

```typescript
player.currentMonth += 1
player.totalMonths += 1
```

---

## Механики покупки и продажи

### Покупка за наличные

```typescript
if (player.cash >= property.purchasePrice) {
  player.cash -= property.purchasePrice;
  player.properties.push(property);
  player.experience += 25; // Опыт за покупку
  player.stats.propertiesOwned = max(stats.propertiesOwned, properties.length);
}
```

### Покупка в ипотеку

```typescript
downPayment = property.purchasePrice * 0.2; // 20%
loanAmount = property.purchasePrice - downPayment;
monthlyPayment = calculateAnnuityPayment(loanAmount, interestRate, 120);

if (player.cash >= downPayment) {
  player.cash -= downPayment;
  player.properties.push(property);
  player.loans.push(newLoan);
  property.mortgageId = loan.id;
}
```

### Продажа (flip стратегия)

1. Игрок выбирает стратегию "flip"
2. Устанавливается `salePrice` (по умолчанию = `currentValue`)
3. Объект помечается `isForSale = true`
4. Каждый месяц проверяется вероятность продажи
5. При продаже:
   - Рассчитывается налог: `(salePrice - purchasePrice) * 0.13`
   - Выплачивается кредит (если есть)
   - Объект удаляется

---

## Система кредитования

### Расчет аннуитетного платежа

```typescript
monthlyRate = annualRate / 100 / 12;
payment = principal * (monthlyRate * (1 + monthlyRate)^termMonths) / 
          ((1 + monthlyRate)^termMonths - 1);
```

### Ипотека при покупке

- **Первоначальный взнос**: 20%
- **Срок**: 120 месяцев
- **Ставка**: Зависит от сложности (9.5% / 12.5% / 15.5%)

### Залог под объект

- **Максимальная сумма**: 60% от `currentValue`
- **Срок**: 60 месяцев
- **Ставка**: Базовая ставка + 2%
- **Условие**: Объект не должен иметь ипотеку

---

## Ремонт и улучшение объектов

### Типы ремонта

1. **Косметический ремонт**:
   - Стоимость: 5% от `purchasePrice`
   - Длительность: 1 месяц
   - Увеличение стоимости: +10%
   - Опыт: +40 XP

2. **Капитальный ремонт**:
   - Стоимость: 15% от `purchasePrice`
   - Длительность: 3 месяца
   - Увеличение стоимости: +20%
   - Опыт: +75 XP

### Улучшение состояния

После завершения ремонта состояние улучшается:
- "убитая" → "требует ремонта"
- "требует ремонта" → "нормальная"
- "нормальная" → "после ремонта"

### Во время ремонта

- Объект не сдается в аренду
- Объект не продается
- Расходы на содержание продолжают списываться

---

## Рыночная динамика

### Фазы рынка

#### Рост
- Цены растут медленно (+0.5% в месяц, максимум +30%)
- Аренда растет (+0.3% в месяц, максимум +20%)
- Простой снижается (минимум 2%)

#### Стабильность
- Цены стремятся к базовому уровню
- Аренда стабильна
- Простой около 5%

#### Кризис
- Цены падают (-0.5% в месяц, минимум -30%)
- Аренда падает (-0.3% в месяц, минимум -20%)
- Простой растет (максимум 15%)

### Рыночные события

События активируются в определенные месяцы и влияют на рынок:

**Базовые события:**
- Зимний туристический сезон (месяц 2, 4 месяца): +15% аренда, -10% простой
- Лёгкий кризис (месяц 8, 6 месяцев): -10% цены, -5% аренда, +10% простой
- Запуск нового ТЦ (месяц 12, 8 месяцев): +5% цены, +5% аренда, -5% простой

**Расширенные события:**
- Повышение ключевой ставки: +2% к ставкам по ипотеке, -3% цены
- Снижение ключевой ставки: -1.5% к ставкам, +2% цены
- Строительство нового ЖК: -8% цены в спальных районах
- Расширение порта: +15% цены, +20% аренда для коммерции у порта
- Аномальная зима: -15% аренда, +15% простой
- Новый закон об аренде: +10% аренда, -5% простой
- Сезон ремонтов: +20% стоимость ремонта

---

## Система рисков

### Типы рисков

1. **Протечка в квартире**:
   - Стоимость устранения: 3% от `purchasePrice`
   - Влияние: Требует ремонта, -5% к стоимости
   - Условие: Состояние "нормальная"

2. **Арендатор съехал**:
   - Стоимость: 0
   - Влияние: 2 месяца без аренды
   - Условие: Стратегия "rent"

3. **Поломка коммуникаций**:
   - Стоимость: 2% от `purchasePrice`
   - Влияние: -3% к стоимости

4. **Затопили соседи**:
   - Стоимость: 4% от `purchasePrice`
   - Влияние: Требует ремонта, -8% к стоимости

5. **Проверка налоговой**:
   - Стоимость: 5% от `purchasePrice`
   - Влияние: Штраф

### Вероятность риска

- **Базовая вероятность**: 5% каждый месяц
- Не проверяется для объектов на продаже или в ремонте

### Устранение риска

Игрок может:
1. **Исправить** (заплатить стоимость) → Риск устранен
2. **Игнорировать** → Ситуация может ухудшиться
3. **Отложить** → Риск остается, но не ухудшается

---

## Геймификация: уровни, миссии, достижения

### Система опыта (XP)

Опыт начисляется за:
- **Покупку объекта**: +25 XP
- **Продажу объекта**: +50 XP
- **Аренду**: `Math.floor(income / 1000)` XP
- **Косметический ремонт**: +40 XP
- **Капитальный ремонт**: +75 XP
- **Выполнение миссии**: Зависит от миссии (250-500 XP)
- **Разблокировка достижения**: +200 XP
- **Владение объектами**: +10 XP за каждый объект

### Система уровней

```typescript
levels = [
  { level: 1, exp: 0,      title: 'Начинающий инвестор' },
  { level: 2, exp: 500,    title: 'Новичок' },
  { level: 3, exp: 1500,   title: 'Опытный инвестор' },
  { level: 4, exp: 3000,   title: 'Рантье' },
  { level: 5, exp: 5000,   title: 'Профессионал' },
  { level: 6, exp: 7500,   title: 'Магнат' },
  { level: 7, exp: 10000, title: 'Титан недвижимости' },
  { level: 8, exp: 15000, title: 'Король рынка' },
  { level: 9, exp: 20000, title: 'Легенда инвестиций' },
  { level: 10, exp: 30000, title: 'Властелин недвижимости' }
]
```

### Миссии

1. **Портфель 10 млн**:
   - Цель: Достигнуть чистого капитала 10 000 000 ₽
   - Награда: 500 XP

2. **Аренда 150 000₽/мес**:
   - Цель: Получать 150 000 ₽ аренды в месяц
   - Награда: 300 XP

3. **Все районы**:
   - Цель: Купить объект в каждом районе (4 района)
   - Награда: 400 XP

4. **Портфель из 5 объектов**:
   - Цель: Владеть одновременно 5 объектами
   - Награда: 250 XP

### Достижения

1. **Инвестор-новичок** / **Первый шаг**:
   - Условие: Купить первый объект
   - Иконка: 🏠 / 🎯

2. **Король аренды**:
   - Условие: Получать 200 000 ₽ аренды в месяц
   - Иконка: 👑

3. **Флип-мастер**:
   - Условие: Продать 10 объектов
   - Иконка: 🔄

4. **Магнат порта**:
   - Условие: Владеть 3 коммерческими объектами возле порта
   - Иконка: 🚢

5. **Миллионер**:
   - Условие: Достигнуть капитала 5 000 000 ₽
   - Иконка: 💰

---

## Расширенные рыночные события

### Типы расширенных событий

1. **key_rate_change** (Изменение ключевой ставки):
   - Влияет на ставки по ипотеке
   - Может повышать или понижать ставки на 1.5-2%

2. **new_construction** (Строительство нового ЖК):
   - Влияет на конкретные районы и типы объектов
   - Снижает цены на вторичку

3. **port_expansion** (Расширение порта):
   - Влияет на коммерцию возле порта
   - Резко повышает цены и аренду

4. **anomalous_winter** (Аномальная зима):
   - Снижает туристический спрос
   - Падает аренда

5. **rental_law** (Новый закон об аренде):
   - Защита арендаторов
   - Растет аренда

6. **repair_peak** (Сезон ремонтов):
   - Увеличивает стоимость ремонта на 20%
   - Влияет через `repairCostMultiplier`

---

## Переговоры и торг

### Механика переговоров

При покупке объекта игрок может предложить цену в диапазоне 90-110% от базовой цены.

### Вероятность принятия

```typescript
baseChance = 0.5; // 50% базовая вероятность

if (discountPercent > 0) {
  // Предложение ниже базовой цены
  acceptChance = max(0.1, 0.5 - (discountPercent / 10));
  
  // Модификатор сложности
  if (difficulty === 'hard') acceptChance *= 0.7;
  if (difficulty === 'easy') acceptChance *= 1.2;
} else if (discountPercent < 0) {
  // Предложение выше базовой цены - всегда принимается
  acceptChance = 1.0;
}
```

### Компромисс

Если продавец отклонил предложение, есть 30% шанс предложить компромисс:
- Компромиссная цена = `basePrice - (discount * 0.5)`

---

## Формулы и расчеты

### Чистый капитал

```typescript
netWorth = cash + sum(property.currentValue) - sum(loan.remainingPrincipal)
```

### Месячный доход

```typescript
monthlyIncome = sum(rentIncome) - sum(monthlyExpenses) - sum(loanPayments)
```

### Обновление стоимости объекта

```typescript
phaseMultiplier = {
  "рост": 1.01,
  "стабильность": 1.0,
  "кризис": 0.98
}

newValue = currentValue * phaseMultiplier * 
           product(1 + event.priceImpactPercent / 100) * 
           priceIndex

minValue = purchasePrice * 0.7
newValue = max(newValue, minValue)
```

### Доход от аренды

```typescript
rent = baseMonthlyRent * rentIndex * 
       product(1 + event.rentImpactPercent / 100)

if (random < vacancyRate) {
  income = 0; // Простой
} else {
  income = rent - monthlyExpenses;
}
```

### Налог на прибыль

```typescript
profit = salePrice - purchasePrice - renovationCost
if (profit > 0) {
  tax = profit * 0.13; // 13%
} else {
  tax = 0;
}
```

---

## Развитие игрока

### Прогрессия опыта

Игрок получает опыт за различные действия, что позволяет повышать уровень и получать новые титулы.

### Прогрессия капитала

- Начальный капитал: 1 500 000 ₽ (normal)
- Цель миссии: 10 000 000 ₽
- Нет верхнего предела

### Прогрессия портфеля

- Начальный портфель: 0 объектов
- Цель миссии: 5 объектов одновременно
- Нет верхнего предела

### Прогрессия доходов

- Начальный доход: 0 ₽/мес
- Цель миссии: 150 000 ₽/мес
- Нет верхнего предела

---

## Особенности реализации

### Автоматический игровой цикл

- Игра автоматически переходит к следующему месяцу каждую реальную минуту
- Игрок не может вручную переключать месяцы
- Все расчеты происходят автоматически

### Хранение данных

- Все данные хранятся в памяти (React state)
- При перезагрузке страницы игра начинается заново
- Нет сохранения прогресса (можно добавить localStorage)

### Генерация объектов

- Начальные объекты заданы статически
- При продаже объекты удаляются из рынка
- Новые объекты не генерируются автоматически (можно добавить)

---

## Заключение

Игра представляет собой комплексный симулятор инвестиций в недвижимость с:
- Динамическим рынком с фазами и событиями
- Реалистичными механиками кредитования и налогообложения
- Системой рисков и их устранения
- Геймификацией через уровни, миссии и достижения
- Автоматическим прогрессом времени

Все механики сбалансированы для создания интересного и реалистичного игрового опыта.

