# Миграция на реальное время - Статус и инструкции

## ✅ Что уже сделано

### 1. Обновлены типы данных (`src/types.ts`)

- ✅ Добавлен интерфейс `City` для поддержки городов
- ✅ Добавлен интерфейс `GameTimers` для конфигурации таймеров
- ✅ Обновлен `Property`:
  - Добавлены поля: `cityId`, `baseRent` (вместо `baseMonthlyRent`), `rentIntervalMs`, `nextRentAt`, `renovationEndsAt`
  - Поля `strategy` теперь не может быть `null`, только `"none" | "hold" | "rent" | "flip"`
  - Старые поля помечены как deprecated, но оставлены для обратной совместимости
- ✅ Обновлен `Loan`:
  - Добавлены поля: `playerId`, `annualRate`, `paymentIntervalMs`, `nextPaymentAt`
  - Старые поля (`interestRate`, `remainingTermMonths`) помечены как deprecated
- ✅ Обновлен `MarketEvent`:
  - Добавлены поля: `cityId`, `startsAt`, `endsAt`, `priceIndexModifier`, `rentIndexModifier`, `vacancyModifier`
  - Старые поля (`monthImpactStart`, `durationMonths`, `priceImpactPercent` и т.д.) помечены как deprecated
- ✅ Обновлен `MarketState`:
  - Добавлены поля: `cityId`, `phase` (вместо `currentPhase`), `lastUpdatedAt`
- ✅ Обновлен `Player`:
  - Добавлены поля: `cityId`, `lastSyncedAt`, `createdAt`, `softCurrency`, `hardCurrency`
  - Старые поля (`currentMonth`, `totalMonths`) помечены как deprecated
- ✅ Обновлен `GameEvent`:
  - Добавлено поле `timestamp` (опционально для обратной совместимости)
  - Поле `month` помечено как deprecated
- ✅ Обновлен `PropertyRisk`:
  - Добавлено поле `timestamp` (опционально)
  - Поле `rentPeriodsWithoutIncome` вместо `monthsWithoutRent`

### 2. Создана новая логика реального времени (`src/utils/realtimeLogic.ts`)

- ✅ Функция `processOfflinePeriod()` - обрабатывает пропущенные периоды при входе в игру
- ✅ Функция `processRealtimeTick()` - обрабатывает один период реального времени
- ✅ Функции покупки/продажи для реального времени:
  - `buyPropertyWithCashRealtime()`
  - `buyPropertyWithMortgageRealtime()`
  - `changePropertyStrategyRealtime()`
  - `startRenovationRealtime()`
  - `takeLoanAgainstPropertyRealtime()`
- ✅ Константы таймеров `DEFAULT_TIMERS`

### 3. Создана система миграции данных (`src/utils/dataMigration.ts`)

- ✅ Функции миграции старых данных в новый формат:
  - `migratePlayerToRealtime()`
  - `migratePropertyToRealtime()`
  - `migrateLoanToRealtime()`
  - `migrateMarketToRealtime()`
  - `migrateEventToRealtime()`
  - `migrateGameEventToRealtime()`

### 4. Создана система городов (`src/data/cities.ts`)

- ✅ Данные о городах (пока только Мурманск)
- ✅ Функции `getCityById()` и `getDefaultCity()`

### 5. Обновлена инициализация

- ✅ `createInitialPlayer()` теперь создает Player с новыми полями
- ✅ `initializeMarket()` теперь принимает `cityId` и создает MarketState с новыми полями

## ⚠️ Что нужно доделать

### 1. Обновить использование старых полей в компонентах

Нужно заменить во всех компонентах:
- `property.baseMonthlyRent` → `property.baseRent`
- `property.renovationMonthsLeft` → использовать `property.renovationEndsAt` и вычислять разницу
- `property.isForSale` → `property.strategy === "flip"`
- `property.mortgageId` → `property.loanId`
- `market.currentPhase` → `market.phase`
- `event.month` → `event.timestamp`
- `risk.monthsWithoutRent` → `risk.impact.rentPeriodsWithoutIncome`

**Файлы для обновления:**
- `src/components/mobile/Dashboard.tsx`
- `src/components/mobile/MarketScreen.tsx`
- `src/components/mobile/PropertyDetailModal.tsx`
- `src/components/mobile/RiskResolutionModal.tsx`
- `src/components/PropertiesList.tsx`
- `src/components/MarketProperties.tsx`
- `src/components/EventsLog.tsx`
- `src/components/mobile/EventsScreen.tsx`

### 2. Обновить mockData

- `src/data/mockData.ts`:
  - Обновить `initialMarketProperties` - добавить `cityId`, `baseRent`, таймеры
  - Обновить `mockMarketEvents` - использовать новые поля с timestamps
- `src/data/extendedEvents.ts`:
  - Обновить все события для использования новых полей (`cityId`, `startsAt`, `endsAt`, модификаторы)

### 3. Обновить App.tsx

- Заменить `processMonth()` на `processRealtimeTick()`
- Добавить обработку офлайн-периодов при загрузке игры
- Обновить таймеры для использования реального времени
- Использовать новые функции покупки/продажи из `realtimeLogic.ts`
- Обновить все места, где создаются `GameEvent`, чтобы включать `timestamp`

### 4. Обновить calculations.ts

- Функция `calculateRentIncome()` должна работать с новыми полями
- Учесть таймеры и реальное время

### 5. Обновить marketLogic.ts

- Функция `checkAndActivateEvents()` должна работать с timestamps вместо месяцев
- Функция `updateMarketPhase()` должна обновлять `phase` вместо `currentPhase`

### 6. Обновить propertyRisks.ts

- Функция `checkPropertyRisks()` должна использовать `timestamp` вместо `month`
- Обновить создание рисков для использования новых полей

## 📋 План миграции

### Этап 1: Обновление данных (приоритет: высокий)
1. Обновить `mockData.ts` и `extendedEvents.ts`
2. Обновить все компоненты для использования новых полей

### Этап 2: Переход на реальное время (приоритет: высокий)
1. Обновить `App.tsx` для использования `processRealtimeTick()`
2. Добавить обработку офлайн-периодов
3. Обновить таймеры

### Этап 3: Обновление логики (приоритет: средний)
1. Обновить `calculations.ts`
2. Обновить `marketLogic.ts`
3. Обновить `propertyRisks.ts`

### Этап 4: Тестирование и оптимизация (приоритет: средний)
1. Протестировать все функции
2. Оптимизировать производительность
3. Добавить обработку ошибок

## 🔧 Конфигурация таймеров

По умолчанию:
- **Аренда**: 1 минута = 1 игровой месяц
- **Кредитные платежи**: 1 минута = 1 игровой месяц
- **Проверка ремонта**: 1 секунда
- **Обновление рынка**: 1 минута

Эти значения можно изменить в `DEFAULT_TIMERS` в `src/utils/realtimeLogic.ts`.

## 🚀 Интеграция с бэкендом (будущее)

Когда будет готов бэкенд:

1. **Синхронизация состояния**:
   - При входе вызывать `processOfflinePeriod()` с данными с сервера
   - Регулярно синхронизировать состояние с сервером

2. **API эндпоинты** (пример):
   - `GET /api/player/{id}` - получить состояние игрока
   - `POST /api/player/{id}/sync` - синхронизировать состояние
   - `GET /api/cities` - получить список городов
   - `GET /api/market/{cityId}` - получить состояние рынка
   - `POST /api/property/buy` - купить объект
   - `POST /api/property/sell` - продать объект

3. **WebSocket** (опционально):
   - Реал-тайм обновления рынка
   - Уведомления о событиях
   - Обновления других игроков

## 📝 Примечания

- Все старые поля помечены как `deprecated`, но оставлены для обратной совместимости
- Миграция данных происходит автоматически через функции в `dataMigration.ts`
- Для полной миграции нужно обновить все компоненты и данные
- После миграции можно будет удалить deprecated поля

## 🎯 Следующие шаги

1. Обновить все компоненты для использования новых полей
2. Обновить mockData и extendedEvents
3. Переключить App.tsx на использование realtimeLogic
4. Протестировать все функции
5. Подготовить к интеграции с бэкендом

